#include "monty.h"

/**
 * f_nop - No operation (does nothing).
 * @head: Stack head.
 * @counter: Line number.
 * Return: No return value.
 */
void f_nop(stack_t **head, unsigned int counter)
{
	(void) counter;
	(void) head;
}

